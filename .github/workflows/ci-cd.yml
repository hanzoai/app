name: Docker
on:
  workflow_dispatch:
  push:
    branches: [main, develop, dev, test]
    tags: ['v*']
  pull_request:
    branches: [main]
permissions:
  contents: read
  packages: write
  id-token: write
jobs:
  docker:
    if: github.event_name != 'pull_request'
    uses: hanzoai/.github/.github/workflows/docker-build.yml@main
    with:
      image: ghcr.io/hanzoai/build
      dockerfile: Dockerfile.production
    secrets: inherit
